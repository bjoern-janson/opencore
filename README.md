# OpenCore

> Experimental substrate for **bounded epistemic compounding**: useful persistent state without self-erasure of the causal routes required for correction.

OpenCore studies how adaptive systems can accumulate persistent causal leverage while remaining exposed to externally grounded consequence capable of reducing that leverage.

This branch is the active experimental lineage. The original K0 charter, kernel contract, and E001 specification remain frozen historical artifacts and are not rewritten by crank results.

## Current status

| Field | Current status |
| --- | --- |
| Research object | Boundary validity under persistent epistemic compounding |
| Current causal result | `PCE-001`: protected corrective exposure at externally constituted `D_t^oracle` |
| Current L2 assay | `CSD-001`: challenge-set sufficiency diagnosis apparatus validated |
| First L2 target result | Unchanged Mini core: `FAILURE_CAUSE_CONFLATION` |
| Post-negative archaeology | No admissible pre-CSD L2 target identified in the audited repository surface |
| Current evidence boundary | **L1 supported at oracle scope; L2 open; L3 untouched** |
| Current research state | **Research freeze** — no further conceptual layer gains authority without new organism evidence or a prospectively frozen developmental experiment |
| Architecture status | No Nano V1, challenge generator, meta-exploration module, procedure-failure primitive, or L2 mechanism earned |
| Formal K0/E001 status | E001 remains separate and unexecuted by the crank lineage |

Canonical living freeze: [crank/RESEARCH_FREEZE.md](crank/RESEARCH_FREEZE.md).

Frozen assay/spec/result artifacts remain authoritative over living summaries.

## Current developmental ladder

```text
S1  compress history into operational state
S2  retain sufficient substrate for authorized reopening
S3  preserve reachable exposure to defeating consequence
S4  recognize endogenously that the current epistemic boundary/procedure is inadequate
S5  construct a better boundary/procedure
```

Current evidence:

```text
S1/S2  supported only within the tested FOREIGN-004/005/006 families
S3     causally supported by PCE-001 at externally constituted D_t^oracle
S4     open; CSD-001 Mini target is a negative
S5     untouched
```

This is a descriptive evidence ladder, not a universal developmental theorem.

## Current causal architecture

The living synthesis is:

```text
H_t
-> Q_t
-> S_t
-> authority
-> pi_t^measure
-> O_{t+1}
-> W_{t+1}
-> E_{t+1}
```

where:

- `H_t` — historical / causal substrate;
- `Q_t` — active quotient / representation boundary;
- `S_t` — active persistent epistemic state;
- `pi_t^measure` — policy governing evidence acquisition / measurement;
- `O_{t+1}` — externally generated consequence reached under that policy;
- `W_{t+1}` — warrant / recognition judgment;
- `E_{t+1}` — revised persistent leverage / authority state.

The central causal distinction remains:

```text
exogenous outcome generation
!=
exogenous outcome exposure
```

External truth can remain independent while the system's exposure to it becomes endogenous.

## Two foreclosure surfaces

For live alternatives `h_i, h_j`, distinguish:

```text
D_t^X
    physically discriminating interventions

D_t^Pi
    interventions whose discrimination survives the current representation
```

The lineage now separates two wounds:

```text
representational foreclosure:
D_t^X != empty
D_t^Pi = empty

exposure foreclosure:
D_t^Pi != empty
but effective policy reachability of D_t^Pi becomes negligible or absent
```

Compactly:

```text
Q_t              -> distinction loss
pi_t^measure     -> exposure loss to surviving distinctions
```

These are independent. Preserving a discriminator does not preserve exposure to it, and exposure cannot recover a distinction already erased by the active representation.

## Self-sealing epistemic compression

The strongest current description of the coupled pathology is:

```text
Q_t
-> pi_t^measure
-> reduced defeating evidence
-> continued apparent warrant for Q_t
```

Name:

```text
self-sealing epistemic compression
```

This is a bounded causal synthesis of the FOREIGN-007 -> BLITZ -> PCE sequence, not a claim that every learner or compression becomes self-sealing.

## Representation / historical compression

The representation line established several bounded distinctions.

### FOREIGN-004

Different histories do not automatically require different persistence identities when they are equivalent over the prospectively frozen future-consequence surface.

```text
different history
!=
automatically different current persistence identity
```

### FOREIGN-005

A safe current quotient need not imply irreversible destruction of the historical substrate required by a later interface.

```text
current-state sufficiency
!=
destructive forgetting
```

### FOREIGN-006

On one bounded binary-linear future-interface family, exact blind reopenability compressed from 64 raw history bits to 6 independent future-distinguishing dimensions.

```text
reopenability can be compressed relative to a bounded future-interface class
```

No universal `memory = rank` theorem follows.

The living compression is therefore:

```text
minimal active state
!=
destructive historical erasure
```

## Exposure / corrective reachability

### FOREIGN-007

A quotient that was safe over the complete current action surface later changed policy support so that a uniquely discriminating probe remained physically available but was not selected.

Earned local structure:

```text
representation
-> policy support
-> corrective exposure
```

### BLITZCRANK-001

Ordinary persistent learning improved performance while eliminating a later-informative probe from greedy support.

```text
useful persistent capability
can coexist with
informative-action support contraction
```

### BLITZCRANK-002

Ordinary consequence eventually restored probe support, but recovery could be slow and first reopening need not be sustained.

```text
first reopening
!=
sustained corrective exposure
```

### PCE-001

PCE-001 prospectively fixed:

```text
D_t^oracle = D_t^Pi
```

and manipulated challenge selection only.

Primary result:

```text
ENDOGENOUS finite T_C:   0 / 256
HAZARD finite T_C:     195 / 256 = 76.171875%
```

Secondary result:

```text
SUPPORT finite T_C:  64 / 256 = 25.000000%
FLOOR finite T_C:   205 / 256 = 80.078125%
```

Thus, on the frozen family:

```text
nonzero challenge support
!=
timely corrective exposure
```

Matched null/informative/corrective worlds also preserve:

```text
information
!=
correction
!=
persistent-leverage revision
```

PCE-001 licenses only the scoped causal statement:

> Within the tested constructed family, persistent learned state can causally reduce exposure to a prospectively identified corrective channel, and exogenizing or quantitatively protecting that exposure can materially reduce correction latency.

## Corrective option reachability

PCE-001 motivates distinguishing arbitrary action availability from corrective option reachability.

Descriptive object:

```text
O_t^corr = { a : a can generate warrant-relevant contradiction }
```

The hierarchy is:

```text
physical action availability
!=
corrective-option availability
!=
timely corrective-option reachability
```

This is not a universal exploration mandate.

## Current L1 / L2 / L3 boundary

The challenge problem remains separated into:

```text
L1  challenge preservation given D_t^oracle
L2  endogenous recognition that the current challenge/evidence-acquisition procedure is inadequate
L3  construction of a better challenge set / procedure
```

PCE-001 supports **L1 only under an externally supplied challenge set**.

CSD-001 then provided a prospectively frozen L2 discrimination surface rather than a pre-authorized L2 mechanism.

## CSD-001 — first L2 target negative

The CSD apparatus was prospectively frozen and validated before target selection.

Unchanged Mini core was selected only because it exposed a native state that could be measured without the adapter computing its meaning:

```text
challenge_buffers / needs_probe(scope)
```

The target trial classified:

```text
FAILURE_CAUSE_CONFLATION
```

Decisive adequate-set control:

```text
F2 persistent needs_probe(c2): 204 / 256
```

and every F3 target seed was premature relative to the frozen warrant boundary:

```text
T_D_candidate = 6
T_W           = 24
```

The result therefore rejects:

```text
needs_probe(c2)
=>
challenge-set / procedure inadequacy
```

The state remains evidence only of local challenge/replacement underdetermination on the tested transport.

Earned distinction:

```text
R3 measurability
!=
L2 semantic sufficiency
```

See [crank/CSD_001.md](crank/CSD_001.md).

## Mini-006 complementary boundary

The post-negative archaeology identified Mini-006 as the strongest semantic near-match.

It contains procedure-level state:

```text
ClosurePolicy.require_explicit_contract
```

but the transition is triggered only after an externally supplied:

```text
PolicyEvent.kind = POLICY_LEVEL_SURPRISE
```

Therefore:

```text
procedure-level semantic state
!=
endogenous procedure-level diagnosis
```

Using Mini-006 as a CSD target would move the L2 classification into transport. It remains inadmissible as a successor target.

See [crank/CSD_001_PROCEDURE_ADEQUACY_ARCHAEOLOGY.md](crank/CSD_001_PROCEDURE_ADEQUACY_ARCHAEOLOGY.md).

## The unresolved L2 transition

The key separation is now:

```text
object uncertainty
!=
procedure efficacy
!=
procedure authority
```

The open L2 transition is:

```text
raw consequence
-> violation of a native warranted procedural expectation
-> causal withdrawal of authority from the current epistemic procedure
```

The missing conjunction is:

```text
native procedural expectation
+
native raw performance evidence
+
endogenous mismatch recognition
+
causal authority withdrawal
```

with no externally supplied procedure-failure classification.

Bounded archaeology result:

> **No admissible pre-CSD L2 target was identified in the audited repository surface.**

This is not an impossibility theorem.

## Nano boundary

[Nano](crank/nano.py) remains deliberately narrow:

```text
transition typechecker + append-only in-process journal
```

It constrains persistent effects under externally supplied contracts. It does not establish:

- truth;
- correctness of a license;
- adequacy of a representation;
- which experiment should run;
- which distinction is missing;
- whether a challenge set is sufficient;
- whether a corrective path remains policy-reachable;
- whether an evidence-acquisition procedure has failed its own efficacy expectation.

Therefore:

```text
Nano protects authority acquisition
!=
Nano guarantees evidence exposure
!=
Nano supplies L2
```

No Nano V1 is earned.

## Living synthesis records

- [crank/RESEARCH_FREEZE.md](crank/RESEARCH_FREEZE.md) — current research freeze and evidence boundary.
- [crank/CSD_001.md](crank/CSD_001.md) — CSD apparatus and first Mini target negative.
- [crank/CSD_001_TARGET_SELECTION.md](crank/CSD_001_TARGET_SELECTION.md) — pre-CSD target-selection audit.
- [crank/CSD_001_PROCEDURE_ADEQUACY_ARCHAEOLOGY.md](crank/CSD_001_PROCEDURE_ADEQUACY_ARCHAEOLOGY.md) — post-negative semantics-only archaeology.
- [crank/QUOTIENT_PRESSURE_LINEAGE.md](crank/QUOTIENT_PRESSURE_LINEAGE.md) — quotient pressure through FOREIGN-004.
- [crank/REOPENABILITY_LINEAGE.md](crank/REOPENABILITY_LINEAGE.md) — FOREIGN-005/006 informational reopenability and capacity record.
- [crank/TOPOLOGICAL_REOPENABILITY_LINEAGE.md](crank/TOPOLOGICAL_REOPENABILITY_LINEAGE.md) — FOREIGN-007 through the learned-policy bridge.
- [crank/CORRECTIVE_EXPOSURE_LINEAGE.md](crank/CORRECTIVE_EXPOSURE_LINEAGE.md) — Blitzcrank -> PCE-001 causal intervention synthesis and L1/L2/L3 boundary.

Frozen assay/spec/result artifacts remain authoritative over living summaries.

## Historical K0 artifacts

The following are historical prospective freezes and intentionally remain unchanged:

- [CHARTER.md](CHARTER.md)
- [KERNEL_CONTRACT.md](KERNEL_CONTRACT.md)
- [EXPERIMENT_001.md](EXPERIMENT_001.md)

They describe the pre-code K0/E001 state because that was the state at freeze time. Crank execution does not complete or amend E001.

## Claim ceiling

The repository does **not** establish:

- a universal corrigibility law;
- a universal learner or theory of intelligence;
- universal self-sealing epistemic compression;
- that every informative action should remain supported;
- that exploration is universally beneficial;
- a scalar corrigibility metric;
- a universal memory-capacity/rank theorem;
- autonomous challenge-set diagnosis;
- autonomous challenge constitution;
- L2 impossibility;
- a procedure-level authority mechanism;
- automatic interface invention or repair;
- correctness of externally constituted licenses or challenge sets;
- Nano V1 or a production authority ledger;
- a complete cognitive architecture.

## Research rule and freeze

The governing methodological rule remains:

> **No new semantic primitive without a frozen counterexample, an unrepresentability argument, or a discriminating witness.**

The current authority rule is now stricter:

> **No further conceptual refinement acquires scientific authority until new organism evidence or a prospectively frozen developmental experiment directly bears on the missing L2 transition.**

In particular:

```text
do not manufacture procedure-level authority merely because existing substrates lack it
```

The strongest current bounded conclusion is:

> **Within the audited OpenCore lineage, adaptive learning has produced persistent state, object-level uncertainty, reopening, policy adaptation, and changes in corrective exposure, but has not yet demonstrated endogenous authority over the adequacy of the procedure by which evidence is acquired.**

That is the current research freeze.