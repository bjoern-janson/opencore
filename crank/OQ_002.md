# OQ-002 — Coarse-carrier persistence pressure

**Status:** `PERSISTENCE_WOUND_ESTABLISHED`

## Frozen question

Does the apparatus distinction established by OQ-001 become persistence-relevant when a coarse carrier aliases histories A and B before unchanged Nano receives them?

The only manipulated variable is the event-identity resolver:

```text
coarse:   A,0 -> k      B,0 -> k
complete: A,0 -> k_A    B,0 -> k_B
```

Nano is byte-identical to the frozen OpenCore Nano V0:

```text
SHA-256 8d820d8f8d9021c5b969659a845aefd2a16e39c24834f0a78e4b491c294b0329
```

Nano receives no quantum state, Kraus operator, probability, residue-like semantic object, or instrument ontology. It sees opaque event identities, standings, and externally constituted transition contracts only.

## OQ-001 prerequisite

OQ-001 established that after the same first classical carrier `(Z,0)`:

```text
X profile under A = HALF
X profile under B = CERTAIN_PLUS
Y profile under A = HALF
Y profile under B = HALF
```

Thus X is the prospective discriminator and Y is the prospective null control.

## Contract geometry

History A first establishes a probe-profile standing on the event identity produced by the carrier resolver.

The B-history downstream proposal is held fixed across coarse and complete conditions:

```text
operation = persist-downstream-profile
write     = history:B:downstream / persistent-profile / <probe> = HALF
```

The contract template is also held fixed:

```text
precondition = CURRENT_EVENT_ID / probe-profile / <probe> == HALF
allowed write = downstream profile HALF
```

Only `CURRENT_EVENT_ID` resolves differently:

```text
coarse B -> k
complete B -> k_B
```

The exact Nano transition digest for the B proposal is identical across coarse and complete conditions for each probe.

## X discriminator result

### Coarse carrier

A establishes:

```text
k / X = HALF
```

B later resolves to the same key `k`.

The unchanged Nano precondition therefore sees the A-derived HALF standing as applicable to B and returns:

```text
ALLOW
```

The durable B downstream standing becomes:

```text
HALF
```

but the OQ-001 external truth for B/X is:

```text
CERTAIN_PLUS
```

The ALLOW receipt's warrant parent is exactly the A-profile receipt. Therefore this is not merely a wrong value coinciding with aliasing; the durable B transition is causally authorized through the aliased A standing.

### Complete carrier

A establishes:

```text
k_A / X = HALF
```

B resolves to `k_B`, on which no profile has yet been established. The identical downstream proposal therefore returns:

```text
DEFER
```

After B-specific evidence establishes:

```text
k_B / X = CERTAIN_PLUS
```

the same HALF proposal returns:

```text
DENY
```

and a separately licensed correct B proposal returns:

```text
ALLOW -> CERTAIN_PLUS
```

## Y null control

Under the same coarse aliasing:

```text
A/Y = HALF
B/Y = HALF
```

The B downstream HALF proposal returns:

```text
ALLOW
```

and is externally correct.

Under the complete carrier it initially defers until B-specific evidence exists, then the same HALF proposal is allowed.

Thus apparatus aliasing does not generically create an error. It creates the persistent failure exactly where OQ-001 showed the erased distinction to be consequence-relevant.

## Earned claim

> A quantum-mechanically consequence-relevant apparatus distinction, when erased before the persistence boundary, caused an A-derived standing to satisfy a B-history precondition and authorize a wrong durable X-profile. Preserving opaque apparatus identity prevented that authority transfer, while the Y control remained harmless under the same aliasing.

Equivalent compression:

```text
physical distinction
    -> coarse apparatus quotient
    -> cross-history warrant reuse
    -> wrong durable consequence
```

The localized diagnosis is:

```text
persistent-authority wound caused by premature apparatus quotienting upstream of unchanged Nano
```

## Not earned

OQ-002 does **not** establish that:

- Nano is defective;
- Nano requires quantum semantics;
- OpenCore needs a `post_state` field;
- instrument identity is a universal OpenCore primitive;
- quantum hardware behavior has been tested;
- `QuantumNano` or `QuantumBase` should exist.

Nano faithfully enforced the state and contracts presented to it. The wound was upstream: the coarse apparatus representation made two correction-relevant histories identical before they crossed the persistence membrane.
