# AF3-OC-001 — Prediction / Evidence / Standing Separation Under AF3-Shaped Scientific Payloads

**Status:** `EXECUTED_FROZEN_MEMBRANE_ASSAY_WITH_REAL_AF3_PAYLOADS`  
**AF3 fork:** `NO`  
**AF3 modified:** `NO`  
**OpenCore modified:** `NO`  
**Nano modified:** `NO`  
**Publication:** experimental crank branch

## 1. Question

AF3-OC-001 asks only:

```text
Can unchanged Nano govern authentic AF3-generated artifacts while preserving

PREDICTION != EXTERNAL EVIDENCE != AUTHORIZED STANDING

without interpreting molecular structure or AF3 confidence semantics?
```

It is not an AF3 accuracy benchmark and it does not test whether OpenCore improves AlphaFold 3.

## 2. Frozen boundary

Adapter/output-interface reference:

```text
google-deepmind/alphafold3
c0f97eda2f1f482fd94d3a38bece18c7069b4a5c
```

Frozen Nano SHA-256:

```text
8d820d8f8d9021c5b969659a845aefd2a16e39c24834f0a78e4b491c294b0329
```

The execution runtime did **not** run AF3 model weights. It consumed immutable public AF3 output artifacts conforming to the pinned AF3 output seam. Their embedded producer identities remain part of provenance.

Nano sees only opaque candidate identities plus externally supplied transition contracts.

## 3. Real AF3 payloads

Three heterogeneous public AF3 outputs are used as opaque candidates:

```text
1PHV protein prediction
  structure Git blob: 0bea758ce798e3450571f45c8adf55910aae2900
  embedded producer: AlphaFold-beta-20231127 (3.0.0 @ 2025-04-14 15:50:05)
  global pLDDT: 95.99

ligand/non-polymer prediction
  structure Git blob: 6493f320e156cc708afac5d7308e86700f44c375
  global pLDDT: 65.00
  pTM / ranking score: 0.58 / 0.58

protein + non-polymer prediction
  structure Git blob: b891bcb4d4ce630a1861b664ea710f715ba9bb19
  pTM / ipTM / ranking score: 0.25 / 0.45 / 0.45
```

For every candidate:

```text
T1 store as PREDICTION             -> ALLOW
T2 confidence-only promotion       -> DENY
```

Thus numerical model confidence does not acquire external-validation standing merely by crossing the persistence boundary.

## 4. Independent scoped evidence

For the primary 1PHV specimen, one narrow comparison is computed outside Nano:

```text
PDB: 1HPV
chain: A
residues: 1-8
atoms: C-alpha only
sequence: PRO GLN ILE THR LEU TRP GLN ARG
experimental method: X-RAY DIFFRACTION
experimental resolution: 1.90 Å
```

Kabsch superposition over the eight matched C-alpha positions gives:

```text
RMSD = 0.1870908947871649 Å
```

This becomes an independently constituted evidence event only for that declared geometric scope. It is **not** whole-protein or whole-complex validation.

Nano never sees the coordinates and never computes RMSD.

## 5. Frozen transition matrix

| Transition | Meaning | Expected Nano |
|---|---|---:|
| T1 | store AF3 prediction | ALLOW |
| T2 | prediction/confidence-only promotion | DENY |
| E1 | admit independent scoped measurement | ALLOW |
| T3 | promote using that independent evidence | ALLOW |
| H | derive dependent standing from validated G | ALLOW |
| U | store unrelated standing | ALLOW |
| E2 | admit controlled contradiction fixture | ALLOW |
| T4 | reopen G and revoke prior validation producer | ALLOW |
| T5a | use refutation capability to install G' | DENY |
| T5b | use authentic successor license before successor evidence | DEFER |
| E3 | admit separate successor-support event | ALLOW |
| T5c | install successor after separate support | ALLOW |

The executed matrix matches all prospective expectations.

## 6. High-confidence laundering control

The primary AF3 candidate carries:

```text
global pLDDT = 95.99
```

but T2 still returns:

```text
DENY
effect:write_not_licensed
```

Therefore, on this assay:

```text
high AF3 confidence != external-validation authority
```

The claim concerns transition authority, not the scientific usefulness of pLDDT.

## 7. Warrant ancestry and reopening

T3's validated-G receipt has the real external-measurement receipt as its parent. H then has validated G as its parent.

After T4 revokes only G's validation producer:

```text
G epistemic role = REOPENED        ACTIVE
H derived from G                    DEFERRED
U unrelated                         ACTIVE
AF3 confidence metadata             ACTIVE
```

Thus authority withdrawal propagates through actual warrant ancestry while unrelated standing survives.

The T4 contradiction is a **controlled mechanism fixture**, not an observed scientific refutation of 1PHV.

## 8. Replacement separation

Using the refutation/reopening capability to install successor G' returns `DENY`.

Using the authentic successor license before its own evidence exists returns `DEFER`.

Only after a distinct successor-support event does the matched successor transition return `ALLOW`.

Therefore:

```text
REFUTE(G) != VALIDATE(G')
```

on the frozen assay.

## 9. What is earned

Narrowly, on the tested authentic AF3 payload identities and controlled transition family:

```text
PREDICTION != EXTERNAL EVIDENCE != AUTHORIZED STANDING
```

and unchanged Nano preserved that distinction without interpreting molecular coordinates or model-confidence semantics.

Also supported on the tested family:

```text
high model confidence alone does not acquire external-validation standing
refutation/reopening authority does not acquire replacement authority
warrant withdrawal propagates through actual authority ancestry
```

## 10. What is not earned

AF3-OC-001 does **not** establish:

```text
AF3 accuracy benchmark
whole-structure validation of 1PHV
fresh AF3 inference at c0f97eda in this runtime
automatic scientific validation
correct molecular ontology for Base
OpenCore improvement of AF3
AF3 improvement of OpenCore
prospective experiment selection
closed-loop automated science
Nano V1
```

## 11. Execution limitation

The runtime lacked the AF3 weights, databases, and suitable stock inference installation. Therefore the earned execution statement is:

```text
unchanged Nano was executed against immutable real AF3 output objects conforming
to the pinned AF3 interface, with one independently computed scoped experimental
comparison
```

not:

```text
stock AF3@c0f97eda was executed end-to-end here
```

No substitute or fabricated AF3 structure was generated.

## 12. Publication files

```text
AF3_OC_001.md
 af3_oc_001.py
 results/af3_oc_001_result.json
```

The published harness is the minimal reproducer of the frozen assay and verifies the frozen Nano hash before execution.
