# CSD-001 — Challenge-Set Sufficiency Diagnosis

**Status:** `APPARATUS_READY_FOR_TARGET_FREEZE`  
**Layer:** L2 assay surface  
**Target mechanism:** none  
**Scientific L2 target evaluation:** not run  
**Nano:** absent

## Scientific question

CSD-001 is the prospective assay for the next OpenCore rung:

> Can a system diagnose that its current challenge set is inadequate, after certified use under a prospectively frozen adequacy envelope, without being told the missing discriminator?

The initial implementation deliberately does **not** implement an L2 mechanism. It validates the causal discrimination surface on which a later frozen target can be tested.

## Prospective custody

The complete prospective specification, manifest, analysis plan, result skeleton, and executable apparatus existed before any apparatus-validation output.

Prospective freeze commit:

```text
e4062b344b3c2918b31bde5577db65503972e655
```

Executed-source SHA-256:

```text
CSD_001_SPEC.md
  ee2b14de9e0ab8ee332d3198b676115ed58714c0f69a6af0b1c1ffd0999d3271

CSD_001_MANIFEST.json
  cdbba807bd5587dfa21f892801661a97fb0be7103a3e7a748d2dbda087242f06

CSD_001_ANALYSIS.md
  c7426bea36074a96a1567abf2cf1784f31ed265e2d59e1a95dc99e87f823da4d

csd_001.py
  1899da7f1675d9b0fb691d973351ebe86aca3819e01182f108e7ee515f93a7c9
```

Before execution, the local bytes used for the run were checked against the frozen Git blob identities for all five custody artifacts.

## Frozen adequacy envelope

The load-bearing prospective condition is:

```text
Gamma is constituted independently of realized F1/F2/F3/F3u outcomes.
```

The current agent-visible challenge set is identical across classes:

```text
D_agent = {c0,c1,c2}
```

and:

```text
B = 8 valid certified audit executions per current challenge.
```

Within the frozen assay family, an adequate `D_agent` must produce at least one representation-preserved `h_A/h_B` discriminator by completion of that certificate.

## Failure geometry

```text
F1   adequate set + poor prior execution
F2   adequate set + bounded stochastic miss
F3   inadequate set + complete certified current-set exposure
F3u  truly inadequate set + incomplete evidence of insufficiency
```

The decisive future target comparison remains:

```text
F2 vs F3
```

after identical certified use of the exact same `D_agent`.

`F1` localizes failure-to-use versus failure-of-set.

`F3u` enforces:

```text
truth of insufficiency
!=
warrant for diagnosing insufficiency
```

## Apparatus run

The frozen apparatus was run over:

```text
256 seeds x 4 failure classes = 1024 world records
```

No target mechanism was present.

Result:

```text
APPARATUS_READY_FOR_TARGET_FREEZE
```

Prospective apparatus checks:

```text
16 / 16 PASS
```

### Adequate-set controls

Pre-transition `D_agent` adequacy passed in every class.

Post-transition:

```text
F1 adequate under Gamma: 256 / 256
F2 adequate under Gamma: 256 / 256
```

The prospectively generated F2 first-signal positions were:

```text
c2 use 1: 52
c2 use 2: 57
c2 use 3: 35
c2 use 4: 34
c2 use 5: 16
c2 use 6: 17
c2 use 7: 15
c2 use 8: 30
```

Thus `30 / 256` F2 seeds form the explicit boundary subset where ambiguity survives until the final `c2` use permitted by `Gamma`.

### Inadequate-set condition

For every F3 seed:

```text
current-set discriminator within D_agent: absent
certified audit coverage: complete
T_U: 24
T_W: 24
```

So under the frozen round-robin audit:

```text
T_U = T_W = 24
```

for every F3 world.

This is an apparatus-side warrant time, not an organism diagnosis time. No `T_D` exists because no L2 target was evaluated.

### F3u unwarranted-prefix control

For every F3u seed:

```text
underlying set truly inadequate: yes
current-set ambiguity unresolved: yes
Gamma certificate complete: no
T_U: absent
T_W: absent in scored prefix
```

Therefore the apparatus can distinguish true insufficiency from currently warranted diagnosis.

### Matched exposure control

The certified F2 and F3 action schedules were byte-for-byte equivalent as action sequences for every paired seed:

```text
c0,c1,c2 repeated until B=8 valid uses per challenge
```

This holds L1 exposure fixed during the decisive comparison.

### Generic-failure shortcut control

A target-visible downstream `FAIL` consequence occurred in every failure class.

Thus:

```text
FAIL -> diagnose challenge-set insufficiency
```

would false-positive in all 256 F1, all 256 F2, and all 256 F3u controls.

### Target-visible leakage audit

No harness-only field was present in the target-visible event payload.

In particular, the target was not given:

```text
failure_class
hidden hypothesis
ground-truth adequacy
c_star
warrant_time
```

### Post-endpoint missing-discriminator control

After the scored endpoint only, the harness forced the hidden `c_star` control in F3/F3u.

It returned the already represented signal semantics:

```text
h_A -> SIG_A
h_B -> SIG_B
```

in every tested F3/F3u world.

Therefore the constructed pre-disclosure wound is not fundamental world non-identifiability or signal-representation failure.

The forced path supplies zero L2 credit.

## Timing semantics reserved for target evaluation

A future frozen target must satisfy, for valid F3 diagnosis:

```text
T_U <= T_W <= T_D < T_*
```

with:

```text
T_D < T_W  -> premature diagnosis
T_D >= T_* -> post-disclosure; not L2
```

and report:

```text
T_D - T_W
```

separately rather than collapsing diagnosis into a scalar meta-corrigibility score.

## Artifact custody

Canonical apparatus result:

```text
crank/results/csd_001_apparatus_result.json
SHA-256 1e268db736dffcf89e53feb98c365ce411b33d1a6d777ddeb4158bb96f707855
```

Canonical apparatus trace:

```text
world records: 1024
uncompressed bytes: 12,573,664
uncompressed SHA-256:
  dd7e5ef08da8163c2fe1b1d7431b7f6e26d66f5c5ce7d5684314cbbc475fe1a0

deterministic gzip bytes: 202,603
deterministic gzip SHA-256:
  c37f371ef92945b0fb51ca19ceec066da7cf076f1d593f30cd96732da1623bdd
```

The repository stores the exact harness, compact apparatus result, and trace commitment. The large generated trace need not be treated as an ordinary source artifact to preserve its byte identity.

## What this implementation earned

Only:

```text
CSD-001 apparatus is valid on its frozen self-tests and ready for a separately frozen target adapter.
```

It did **not** establish:

```text
L2
challenge-set insufficiency diagnosis
challenge repair
challenge constitution
c_star discovery
D_agent = D_oracle
```

No mechanism gained authority because no mechanism was tested.

## Next legitimate move

The next scientific step is not to modify this assay from its result.

It is to freeze, separately and prospectively:

1. an exact target organism/version;
2. an adapter mapping existing target behavior/state to current-set adequacy withdrawal;
3. the behavioral consequence required for that withdrawal to count as non-decorative;
4. target evaluation seeds and stopping thresholds.

Only then may CSD-001 produce one of its scientific target result classes:

```text
CHALLENGE_SET_INSUFFICIENCY_DIAGNOSIS_ESTABLISHED
FAILURE_TO_DIAGNOSE
FAILURE_CAUSE_CONFLATION
DECORATIVE_DIAGNOSIS
INVALID
```

The current evidence boundary therefore remains:

```text
L1 supported at oracle scope
L2 apparatus implemented
L2 target capability not yet tested
L3 untouched
```
