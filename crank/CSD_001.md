# CSD-001 — Challenge-Set Sufficiency Diagnosis

**Status:** `MINI_TARGET_EVALUATED`  
**Scientific target classification:** `FAILURE_CAUSE_CONFLATION`  
**Layer:** L2 challenge-set insufficiency diagnosis  
**CSD apparatus:** unchanged and valid  
**Target:** unchanged OpenCore Mini core  
**L2 mechanism added:** none  
**L3:** untouched  
**Nano:** absent

## 1. Scientific question

CSD-001 asks:

> Can an adaptive system diagnose that the challenge set it already possesses has become insufficient, after certified use under a prospectively frozen adequacy envelope, without being told the missing discriminator?

The experimental ladder remains:

```text
L1  preserve a known corrective channel
L2  diagnose that the known challenge set is insufficient
L3  construct a better challenge set
```

CSD-001 tests L2 only.

## 2. Frozen assay geometry

The apparatus was frozen before target selection/evaluation.

```text
D_agent = {c0,c1,c2}
Gamma: B = 8 valid certified audit executions per current challenge

F1   adequate set + poor prior execution
F2   adequate set + bounded stochastic miss
F3   inadequate set + complete certified exposure
F3u  truly inadequate set + incomplete warrant
```

The decisive comparison is `F2 vs F3` under the same certified current-set action schedule.

For every F3 world:

```text
T_U = 24
T_W = 24
T_* = 25
```

A valid first-target L2 candidate therefore had to satisfy `T_D_candidate = 24`. No missing discriminator was available before scoring.

## 3. Apparatus status

The previously frozen CSD apparatus remains valid.

```text
apparatus checks: 16 / 16 PASS
F2 boundary cases with first discriminator at c2-use B: 30 / 256
```

The target run re-executed the apparatus checks before target evaluation and obtained the same all-pass status.

Thus the target result is not attributed to an invalid F1/F2/F3/F3u construction, mismatched F2/F3 exposure, missing-discriminator leakage, or failed post-endpoint discriminator control.

## 4. Target identity and target freeze

Target:

```text
OpenCore Mini core
crank/mini.py
Git blob:
  0837f8a0b75bb548f57d06e0b5c4eb5422559162
SHA-256:
  fd69206eff5443459a8eebed359a301443ae61e92e0e69eb7a1e6ca376ec5e55
```

Frozen target specification:

```text
crank/CSD_001_TARGET_SPEC.md
Git blob:
  fd55b90e4068f7b75f01f2fbcea900c45dd8e153
```

Frozen target runner v2:

```text
crank/csd_001_target_mini.py
freeze commit:
  65173d26480cc94bf4325d723d79c5f5f6989652
Git blob:
  65221fc794f8537532d7ffa1c73571969db319ed
SHA-256:
  1c59f5b46603592797918efb086b1b35b7b269c447df92e37e5f619185a47c9a
```

The runner implements only the frozen mechanical path:

```text
CSD visible event
-> frozen raw transport
-> unchanged Mini.observe(...)
-> unchanged Mini state
-> stateless needs_probe adapter
-> frozen scorer
```

It adds no adequacy evaluator, L2 detector, challenge generator, or repair path.

## 5. Measurement-interface checks

Before generating target evaluation worlds, the runner checked the measurement surface.

```text
source identity checks: 5 / 5 PASS
adapter A1-A4 checks: 7 / 7 PASS
```

The accepted adapter remains exactly the stateless projection:

```text
Y_t = (
  needs_probe(CSD001::c0),
  needs_probe(CSD001::c1),
  needs_probe(CSD001::c2)
)
```

The adapter does not receive Gamma, failure class, coverage status, T_W, c_star, or ground-truth adequacy. The executable A4 counterfactual-invariance test passed, and its deliberately contaminated negative control was detected.

## 6. Frozen target result

The complete target trial ran:

```text
256 seeds × 4 failure classes = 1024 scored worlds
```

Classification:

```text
FAILURE_CAUSE_CONFLATION
```

Frozen endpoint counts:

| class/measure | count | rate |
|---|---:|---:|
| F1 persistent c2 request | 0 / 256 | 0.000000 |
| F2 persistent c2 request | 204 / 256 | 0.796875 |
| F3 persistent c2 request | 256 / 256 | 1.000000 |
| F3u persistent c2 request | 256 / 256 | 1.000000 |
| F3 valid timing | 0 / 256 | 0.000000 |
| F3 premature | 256 / 256 | 1.000000 |

The prospectively frozen control ceilings were:

```text
MAX_FALSE_PER_CONTROL = 12 / 256
MAX_F3_PREMATURE      = 12 / 256
```

Both F2 and F3u exceed the control ceiling massively, and every F3 world is premature. Therefore the positive L2 gate is not close to being satisfied.

## 7. Timing localization

For every F3 seed:

```text
T_D_candidate = 6
T_W           = 24
```

so `T_D_candidate < T_W` in `256 / 256` F3 worlds.

The request is therefore active eighteen audit steps before challenge-set insufficiency becomes warranted under the frozen adequacy envelope.

F3u strengthens the attribution:

```text
persistent c2 request = 256 / 256
T_W absent in scored prefix
```

Thus Mini enters the same native pending-request condition even when the challenge set is truly inadequate but there is not yet enough evidence to warrant diagnosing that fact.

## 8. Adequate-set control localization

F1 is clean:

```text
persistent c2 request = 0 / 256
```

because c2 immediately supplies the expected discriminator during certified audit.

F2 is the decisive negative control:

```text
persistent c2 request = 204 / 256
```

despite every F2 world being prospectively adequate under Gamma.

The persistent-request start distribution among those 204 F2 worlds is:

```text
T_D_candidate = 6   : 169 worlds
T_D_candidate = 12  : 35 worlds
```

The response depends on the local affine observation sequence, not on exhaustion of the complete current challenge topology.

## 9. Mechanistic localization

The result supports the narrow diagnosis:

```text
native local underdetermination request
!=
challenge-set insufficiency diagnosis
```

Mini's pre-existing `needs_probe(c2)` state reacts to local replacement-rule underdetermination inside the c2 scope. It does not wait for, represent, or track the CSD adequacy condition:

```text
complete certified use of D_agent
+
persistent unresolved distinction
+
Gamma exhausted
```

This is exactly why the state activates in both F3 (structurally inadequate current set) and F2 (adequate current set with bounded misses/later discriminators), and why it activates before warrant in F3/F3u.

Failure locus:

```text
apparatus       PASS
measurement     PASS
L1 exposure     held fixed
target state    endogenous and measurable
inference /
mechanism       FAILS to distinguish local underdetermination
                from challenge-topology insufficiency
```

No evidence supports escalating this failure to L3 or modifying Nano.

## 10. Evidential update

The R3 selection result is preserved:

```text
Mini contains a native measurable underdetermination degree of freedom.
```

But CSD-001 now falsifies the stronger candidate interpretation under the frozen transport:

```text
Mini native pending-challenge state
-> bounded L2 challenge-set insufficiency diagnosis
```

That candidate mechanism loses authority.

Equivalently:

```text
R3 measurability
!=
L2 capability
```

This is a clean negative result, not an apparatus failure.

## 11. Provenance note

Before the first target execution, a repository-write operation temporarily replaced `CSD_001_TARGET_SPEC.md` with a truncated prefix.

No Mini × CSD target trajectory was generated or inspected while that corrupted repository state existed.

The exact original frozen target-spec blob:

```text
fd55b90e4068f7b75f01f2fbcea900c45dd8e153
```

was restored before runner v2 was frozen and before the first target evaluation. The runner/source gates and the actual execution used the restored frozen target identity.

This repository event is recorded because custody failures are part of the scientific provenance even when caught before evaluation.

## 12. Result custody

Generated full result:

```text
bytes:
  63,862
SHA-256:
  5cdd1e5fb41907ff4cd3e0e0bbc6425216a7d90db73f7f7c8a8da11c08c805bb
```

Repository result index:

```text
crank/results/csd_001_mini_target_result_index.json
```

Canonical target trace commitment:

```text
world records:
  1,024
uncompressed bytes:
  8,725,772
uncompressed SHA-256:
  6ae6d98a8b37c70de3cd461720333714c82f4fd91ca50f57a84c37f42726950d

deterministic gzip bytes:
  106,763
deterministic gzip SHA-256:
  07a549e9022006530af99ded90670f52042b69034939c506a20640a5e6d77985
```

The repository commits the compact result index and byte commitment for the generated full result/trace.

## 13. Claim ceiling

CSD-001 does **not** establish L2 for Mini.

The maximum supported target statement is:

> Under the frozen CSD-001 raw challenge-observation transport, unchanged OpenCore Mini's pre-existing pending-challenge state did not selectively identify warranted challenge-set insufficiency. It became persistent prematurely in every inadequate-set F3 world, also persisted before warrant in every F3u world, and false-positive persisted in most adequate F2 worlds. The native state therefore remains evidence of local challenge/replacement underdetermination, not bounded evidence of challenge-set insufficiency.

This result does not establish:

```text
no system can perform L2
no modified Mini could perform L2
a particular L2 repair
challenge-set construction
L3
autonomous c_star discovery
universal interface-diagnosis limits
Nano change
```

## 14. Current scientific boundary

```text
PCE-001
  L1 causal result closed

CSD-001 apparatus
  VALIDATED

Mini target selection
  R3 measurement candidate

Mini target trial
  FAILURE_CAUSE_CONFLATION

L2 for unchanged Mini under frozen transport
  NOT ESTABLISHED

L2 mechanism
  NOT PRE-AUTHORIZED

L3
  UNTOUCHED
```

Per the standing epistemic rule, this negative result reduces authority of the tested candidate interpretation. It does not authorize post-hoc mechanism proliferation under CSD-001.
